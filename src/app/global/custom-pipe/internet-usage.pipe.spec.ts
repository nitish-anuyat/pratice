import { InternetUsagePipe } from './internet-usage.pipe';

describe('InternetUsagePipe', () => {
  it('create an instance', () => {
    const pipe = new InternetUsagePipe();
    expect(pipe).toBeTruthy();
  });
});
